package com.company.day2;

import java.util.Scanner;
//Declare a two dimensional array of 5 by 3, find average of each column
public class Array2D {
    public static void main(String[] args) {
         /*   int avg;
           int arr[][] = new int[5][3];
           for(int i=0;i< arr.length;i++){
               int sum = 0;
               sum = arr[i]+i;

               System.out.println("average is :"+sum);*/
           }









    ////sum of each row and sum of each column
       /* Scanner sc = new Scanner(System.in);
        int sumRow=0,sumCol=0;
        System.out.println("Enter the row length of array ");
        int row = sc.nextInt();
        System.out.println("Enter the column length of array ");
        int col = sc.nextInt();
        int[][] arr = new int[row][col];    //declare and initilize
        System.out.print("Enter " + row*col + " Elements to Store in Array :\n");

        for(int i=0;i<row;i++){
            sumRow=0;
            for(int j=0;j<col;j++){
                arr[i][j] = sc.nextInt();
                sumRow = sumRow + arr[i][j];
            }
            System.out.println("Sum of " + (i+1) +" row: " + sumRow);
        }
        for (int i = 0; i < row; i++)
        {
            sumCol=0;
            for(int j = 0; j < col; j++)
            {
               sumCol = sumCol + arr[j][i];
            }
            System.out.println("Sum of " + (i+1) +" col: " + sumCol);
        }
    }*/
    }
